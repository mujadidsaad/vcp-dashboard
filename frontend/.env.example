# Copy this file to .env.production (or set on Vercel dashboard) and point
# it at your deployed backend.
#
# In dev, leave VITE_API_BASE unset — Vite proxies /api/* to
# http://localhost:8000 (see vite.config.ts).
#
# In production:
VITE_API_BASE=https://vcp-screener-api.onrender.com